<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.item.mapper.ItemMapper">

    <resultMap id="BaseResultMap" type="cn.edu.guet.item.domain.Item">
            <id property="id" column="id" />
            <result property="title" column="title" />
            <result property="price" column="price" />
            <result property="description" column="description" />
            <result property="sales" column="sales" />
            <result property="imgUrl" column="img_url" />
    </resultMap>
    <!-- ItemMapper.xml 中 insert 语句的配置 -->
    <insert id="insert" parameterType="cn.edu.guet.item.domain.Item"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO item (title, price, description, img_url)
        VALUES (#{title}, #{price}, #{description}, #{imgUrl})
    </insert>

    <sql id="Base_Column_List">
        id,title,price,description,sales,img_url
    </sql>

</mapper>
